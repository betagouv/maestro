{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "target": "esnext",
    "lib": ["esnext", "ESNext.Array"],
    "typeRoots": ["node_modules/@types", "../types"]
  },
  "include": [
    "**/*.ts",
    "**/*.js",
    "**/*.json",
    "../test/vitest-random-seed.ts"
  ],
  "exclude": ["**/*.d.ts", "dist", "node_modules"]
}
