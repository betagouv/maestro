{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "lib": ["esnext", "ESNext.Array"],
    "typeRoots": ["node_modules/@types",
      "../types"
    ]
  },
  "include": ["**/*.ts", "**/*.js", "**/*.json",
  ],
  "exclude": [
    "**/*.d.ts", "dist", "node_modules"]
}
